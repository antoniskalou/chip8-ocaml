(executable
 (public_name chip8)
 (name main)
 (modules main)
 (libraries
  chip8
  stdint
  tsdl))

(executable
 (name disassembler)
 (modules disassembler)
 (libraries chip8 stdint))

(executable
 (name debugger)
 (modules debugger)
 (libraries chip8))

(executable
  (name sample_audio)
  (modules sample_audio)
  (libraries chip8 tsdl))

(env
  (release
    (ocamlopt_flags
     (:standard -O2 -ccopt -static -ccopt -s))))
